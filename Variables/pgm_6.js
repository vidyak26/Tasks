// Declaring and assigning in two steps

var playerName;
var locationName;
var score; // Declare a score variable

playerName = "Lionel Messi";
locationName = "Argentina";
score = 100; // Assign it a value

console.log(playerName + " is in " + locationName);
console.log(playerName + " has a score of " + score); // Display an extra message on the consoles